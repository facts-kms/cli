# Carry directory names with identity exports.

`fact identity export` now wraps identity bundles with matching directory metadata when available, and `fact identity import` applies that metadata so imported actors keep display names and aliases without granting authority.

