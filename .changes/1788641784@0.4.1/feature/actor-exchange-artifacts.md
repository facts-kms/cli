# Define actor exchange artifacts.

The CLI now has reusable request and response artifact definitions for remote actor bootstrap, including base64url bundle handling, delimiter parsing, wrong-artifact errors, and response grant verification helpers.

