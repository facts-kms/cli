# Scope identity exports to one actor.

`fact identity export` now accepts `--actor`, reports the exported actor references, and describes exported bundles as public identity objects while pointing private key backups at `.facts/identities`.

