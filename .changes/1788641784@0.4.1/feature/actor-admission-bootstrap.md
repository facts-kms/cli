# Add actor admission bootstrap

- Add `fact actor send`, `fact actor inspect`, and `fact actor admit` for exchanging remote-ledger admission artifacts.
- Let admitted actors receive a clone-ready response descriptor with signed grant proof and optional HTTP bearer token.
