# Report actors from identity imports.

`fact identity import` now reports every actor carried by the bundle, including short references and whether each actor was newly imported or already present.

