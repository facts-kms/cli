# Release 0.1.3

- Attaching or using a read-only ledger with no signing identity set no
  longer errors; the CLI now reports that there is no current signer
  (noting when the ledger is read-only) instead of failing.
- Release management has been updated.
